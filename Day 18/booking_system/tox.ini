[tox]
envlist = py39, py310, py311, py312
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    pytest-xdist
    pandas
    numpy
commands =
    pytest -v --cov=src --cov-report=term --cov-report=html tests/